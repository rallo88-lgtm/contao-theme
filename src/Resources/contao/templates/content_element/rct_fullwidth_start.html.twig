{# CSS-Breakout: Section bleibt im Element-Flow und sprengt sich per CSS auf 100vw.
   Schließendes Element via rct_fullwidth_end. #}
<section class="rct-fullwidth{% if cssClass %} {{ cssClass }}{% endif %}"{% if htmlId %} id="{{ htmlId }}"{% endif %}>
