{#
  RCT – fe_page_nav_left.html.twig
  Variante mit festem Layout (sidebar). Theme kommt aus localStorage.
#}
{% extends "@Contao/fe_page.html.twig" %}

{% block head_init %}
{{ parent() }}
<script>
  (function() {
    document.documentElement.setAttribute('data-fixed', '1');
    document.documentElement.setAttribute('data-layout', 'sidebar');
    if (window.innerWidth > 1024 && localStorage.getItem('rct-sidebar-state') === 'closed') {
      document.documentElement.classList.add('rct-left-initial-closed');
    }
  })();
</script>
{% endblock %}

{% block right_sidebar %}
<aside id="right" aria-label="Info-Panel">
  <button id="rct-menu-right-toggle" class="rct-sidebar-close-btn right" title="Schließen">
    <svg viewBox="0 0 24 24" width="20" height="20" stroke="currentColor" stroke-width="2" fill="none">
      <path class="arrow-1" d="M6 6L12 12L6 18" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"></path>
      <path class="arrow-2" d="M12 6L18 12L12 18" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"></path>
    </svg>
  </button>
  <div class="inside">{{ right|raw }}</div>
</aside>
{% endblock %}

{% block header_nav %}
<div class="rct-header-nav">
  <div class="header-controls">
    {{ include('@Contao/_rct_ctrl_nav_toggle.html.twig') }}
    {{ include('@Contao/_rct_ctrl_language.html.twig') }}
    {{ include('@Contao/_rct_ctrl_right_toggle.html.twig') }}
  </div>
</div>
{% endblock %}
